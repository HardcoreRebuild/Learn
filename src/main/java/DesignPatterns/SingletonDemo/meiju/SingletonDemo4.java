package DesignPatterns.SingletonDemo.meiju;
/*
    枚举式单例实现：
    1. 定义一个枚举的元素
* */
public enum SingletonDemo4 {
    // 枚举类即单例模式。定义一个枚举的元素，代表一个实例
    INSTANCE;

    public void singletonOperation(){
        // 对实例的操作
    }
}
